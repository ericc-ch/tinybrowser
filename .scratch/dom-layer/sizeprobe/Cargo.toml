[package]
name = "dom-sizeprobe"
version = "0.1.0"
edition = "2021"

# standalone: excluded from the parent workspace on purpose
[workspace]

[dependencies]
dom = { path = "../../../crates/dom" }
html5ever = "0.39"
markup5ever = "0.39"

[profile.tiny]
inherits = "release"
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = true
