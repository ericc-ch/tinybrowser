[env]
RUST_TEST_THREADS = "1"

# QuickJS-ng is the only C compiled in the graph (rquickjs-sys via cc). C
# unwind tables serve backtraces only: nothing unwinds (panic = "abort",
# setjmp instead of exceptions), so drop them (about 53 KB). Plain [env], no
# force, so an ambient CFLAGS still wins.
CFLAGS = "-fno-unwind-tables -fno-asynchronous-unwind-tables"

# Dev and test links use GNU bfd. The pinned toolchain defaults to rust-lld,
# which rejects a DWARF relocation rustc itself emits in some code shapes
# (`unknown relocation (1875)`, see build.rs); bfd ignores it. The release
# binary's size flags in build.rs select lld later on the command line, and
# the later `-fuse-ld` wins.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=bfd"]

