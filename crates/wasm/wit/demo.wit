package tinybrowser:demo@0.1.0;

interface engine {
    resource tab {
        constructor(url: string, html: string);
        eval: func(source: string) -> result<string, string>;
        pump: func();
        title: func() -> result<string, string>;
        text: func() -> result<string, string>;
    }
}

world demo {
    export engine;
}
