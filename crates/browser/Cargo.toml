[package]
name = "browser"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
dom = { workspace = true }
js = { workspace = true }
net = { workspace = true }
# Exactly html5ever 0.39's own markup5ever; same pin as dom's charter, so
# parser QualNames flow into dom as one type, no conversion layer.
markup5ever = "=0.39.0"
html5ever = "=0.39.0"
tendril = "0.5"
